
  # Convert Design to HTML CSS

  This is a code bundle for Convert Design to HTML CSS. The original project is available at https://www.figma.com/design/NS3b4EQSe7gKmPxLFs4XC0/Convert-Design-to-HTML-CSS.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  